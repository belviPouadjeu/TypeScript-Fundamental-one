// A variable is a named container used to store data or information in a program.

// Define a boolean variable to indicate whether something is found
let found: boolean = true;
// Define a number variable to store the grade of a student
let grade: number = 90.5;

// Define a string variable to store the first name of a person
let firstName: string = "Belvinard";

// Define a string variable to store the last name of a person
let lastName: string = "Pouadjeu";


//console.log("hi ".concat(firstName, " ").concat(lastName));

// Print the value of the boolean variable to the console
console.log(found);

// Print a message to the console that includes the grade
console.log("The grade is " + grade);

// Print a greeting message using template literals that include the first and last name
console.log(`Hi ${firstName} ${lastName}`);


